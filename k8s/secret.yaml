apiVersion: v1
kind: Secret
metadata:
  name: notes-app-secret
  
type: Opaque
data:
  # Base64 encoded PostgreSQL password
  # To encode: echo -n "your-postgres-password" | base64
  POSTGRES_PASSWORD: ""  # Replace with your base64 encoded password
